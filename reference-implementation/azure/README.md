# Azure Reference Implementation

This folder contains sanitized Azure Terraform and Kubernetes reference files for the verified AKS, Workload Identity, ACR, Key Vault and Cloudflare Tunnel pattern.

The files are representative. They omit real subscription IDs, tenant IDs, resource group names, Key Vault names, ACR names, tunnel tokens, kubeconfigs and Terraform state.
